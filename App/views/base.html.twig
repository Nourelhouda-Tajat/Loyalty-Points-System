<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ShopEasy{% endblock %}</title>
    <link rel="stylesheet" href="{{ base_path }}/style.css">
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <header class="header">
        <div class="container">
            <a href="{{ base_path }}" class="logo">ShopEasy</a>
            <nav class="nav">
                {% if user is defined and user %}
                    <span class="user-info">
                        Bonjour, {{ user.name|capitalize }} !
                        
                    <a href="{{ base_path }}/dashboard" class="auth-link">Dashboard</a>
                    <a href="{{ base_path }}/logout" class="auth-link">logout</a>
                {% else %}
                    <a href="{{ base_path }}/login" class="auth-link">log in</a>
                {% endif %}

                <a href="{{ base_path }}/cart" class="cart-link{% if app.current_route == 'cart' %} active{% endif %}">
                    Panier
                    {% if cart_count > 0 %}
                        <span class="cart-count">{{ cart_count }}</span>
                    {% endif %}
                </a>
            </nav>
        </div>
    </header>

    <main class="main">
        <div class="container">
            {% block content %}{% endblock %}
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2026 ShopEasy - Système de test pour formation MVC</p>
            {% block footer_extra %}
                <a href="{{ base_path }}/" class="reset-link">Accueil</a>
            {% endblock %}
        </div>
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>
